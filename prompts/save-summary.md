---
description: Summarize this session and persist it via save_session_summary
---
Save a summary of this session. Use the `save-summary` skill: distill the conversation into a `title`, `description`, and `context`, then call the `save_session_summary` tool exactly once with those three fields. Report the tool's returned message.
